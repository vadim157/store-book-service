
spring:
  cloud:
    stream:
      kafka:
        binder:
          auto-add-partitions: true
      bindings:
        input:
          destination: destination
          group: group
          binder: kafka
server:
  port: 0